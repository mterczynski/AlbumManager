<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="local.terczynski.albummanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Activities.CollageActivity"></activity>
        <activity android:name=".Activities.AlbumsActivity"></activity>
        <activity android:name=".Activities.NotatkiActivity"></activity>
        <activity android:name=".Activities.SeeOnlineActivity"></activity>
        <activity android:name=".Activities.Album_details"></activity>
        <activity android:name=".Activities.PictureDetailsActivity"></activity>
        <activity android:name=".Activities.PictureActivity"></activity>

    </application>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-permission android:name="android.permission.CAMERA" />

</manifest>